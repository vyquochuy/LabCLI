default_role: operator

users:
  admin: admin

roles:
  admin:
    - init
    - backup
    - verify
    - restore
    - audit-verify

  operator:
    - backup
    - verify
    - restore
    - list-snapshots
    - audit-verify
