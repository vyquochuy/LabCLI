default_role: operator

users:
  alice: admin
  bob: operator
  eve: auditor

roles:
  admin:
    - init
    - backup
    - list-snapshots
    - verify
    - restore
    - audit-verify
    - delete-snapshot
    - purge
    - cleanup
    
  operator:
    - backup
    - list-snapshots
    - verify
    - restore
    - audit-verify
    - cleanup
    
  auditor:
    - list-snapshots
    - verify
    - audit-verify